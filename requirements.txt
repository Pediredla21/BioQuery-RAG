# Core dependencies
langchain==0.2.16
langchain-community==0.2.16
langchain-huggingface==0.0.3

# Vector database
faiss-cpu==1.8.0.post1

# PDF loading
pypdf==4.3.1

# Environment variable loading
python-dotenv==1.0.1

# Sentence-Transformer embeddings (all-MiniLM-L6-v2)
sentence-transformers==3.0.1
transformers>=4.45.0
huggingface_hub>=0.26.0

# Groq LLM API client
# groq>=0.13.0 is required — older versions (e.g. 0.9.0) pass a 'proxies'
# argument to httpx that was removed in httpx 0.28.0, causing a TypeError crash.
groq>=0.13.0

# Streamlit UI
streamlit==1.37.1
